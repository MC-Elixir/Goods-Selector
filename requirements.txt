# Core
python-dotenv>=1.0.0
openai>=1.30.0             # PPIO / 任何 OpenAI 兼容视觉接口（首选）
anthropic>=0.40.0          # Claude Vision API（备用）
pyyaml>=6.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.0
# 默认 SQLite，正式环境换 PostgreSQL 时取消下面这行注释
# psycopg2-binary>=2.9.9

# HTTP / API
requests>=2.31.0
httpx>=0.26.0
tenacity>=8.2.0          # 重试装饰器

# Amazon 数据源（三选一，按需安装）
# playwright install chromium  # 爬虫后端（无需 API Key）
playwright>=1.40.0       # playwright install chromium 后可用
scrapling[fetchers]>=0.4.9  # 自适应爬虫 + 反检测（1688 Playwright 后端）
keepa>=1.3.0             # Keepa API（需 KEEPA_API_KEY）
# requests 已在下方 HTTP 节包含，Rainforest 直接用 requests 调用

# 图像处理（图文匹配本地预处理 + 兜底相似度）
Pillow>=10.2.0
imagehash>=4.3.1         # pHash 兜底
# torch>=2.1.0             # 如果走 CLIP 兜底再装
# open-clip-torch>=2.24.0

# 数据处理
pandas>=2.1.0
numpy>=1.26.0

# 报告导出
openpyxl>=3.1.0          # Excel 导出
jinja2>=3.1.0            # Markdown / HTML 模板

# 缓存
diskcache>=5.6.0         # 文件级缓存，避免重复 API 调用

# 日志
loguru>=0.7.0

# CLI
click>=8.1.0
rich>=13.7.0             # 漂亮的终端输出

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
responses>=0.24.0        # mock requests
